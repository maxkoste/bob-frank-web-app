import { Box } from '@mui/material'
import React from 'react'

type Props = {}

function Social({ }: Props) {
    return (
        <Box flex={5} display={"flex"} justifyContent={"flex-end"} padding={2}>
            Social
        </Box>
    )
}

export default Social