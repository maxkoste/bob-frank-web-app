import { Box } from '@mui/material'
import React from 'react'

type Props = {}

function Logo({ }: Props) {
    return (
        <Box flex={5} padding={2}>

            Logo
        </Box>
    )
}

export default Logo